[tool.poetry]
name = "geministationclient"
version = "0.1.0"
description = ""
authors = ["IrisDroidology <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
discord = "^1.0.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
